{
  "$schema": "https://json-schema.org/schema",
  "$id": "Application",
  "title": "Application Generator",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the new application.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "root": {
      "type": "string",
      "description": "The root directory for the application.",
      "default": "apps"
    },
    "path": {
      "type": "string",
      "description": "An optional sub-directory. (Press Enter for none)",
      "x-prompt": "What sub-directory should the app be created in? (optional)",
      "default": ""
    }
  },
  "required": ["name"]
}
