{
  "name": "messenger-settings-ui",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/messenger/ui/settings/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:messenger"],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/libs/messenger/ui/settings",
        "configFile": "libs/messenger/ui/settings/vite.config.mts"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "configFile": "libs/messenger/ui/settings/vitest.config.mts",
        "reportsDirectory": "../../../../coverage/libs/messenger/ui/settings"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
