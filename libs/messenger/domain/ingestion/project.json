{
  "name": "messenger-domain-ingestion",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/messenger/domain/ingestion/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:messenger", "layer:domain"],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/libs/messenger/domain/ingestion",
        "configFile": "libs/messenger/domain/ingestion/vite.config.mts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
