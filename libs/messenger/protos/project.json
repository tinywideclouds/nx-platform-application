{
  "name": "messenger-protos",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/messenger/protos/src",
  "projectType": "library",
  "tags": ["scope:protos-messenger"],
  "targets": {
    "generate": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "rimraf dist",
          "buf generate"
        ],
        "cwd": "libs/messenger/protos"
      },
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
