{
  "name": "contacts-domain-sync",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/contacts/domain/sync/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:contacts", "layer:domain"],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/libs/contacts/domain/sync",
        "configFile": "libs/contacts/domain/sync/vite.config.mts"
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": [
        "{options.reportsDirectory}",
        "{options.htmlReportDirectory}"
      ],
      "options": {
        "reportsDirectory": "coverage/libs/contacts/domain/sync",
        "htmlReportDirectory": "dist/test-reports/libs/contacts/domain/sync",
        "configFile": "libs/contacts/domain/sync/vitest.config.mts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
