{
  "name": "contacts-api",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/contacts/api/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:contacts", "type:public-api"],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/libs/contacts/api",
        "configFile": "libs/contacts/api/vite.config.mts"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "configFile": "libs/contacts/api/vitest.config.mts",
        "reportsDirectory": "coverage/libs/contacts/sttate"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
