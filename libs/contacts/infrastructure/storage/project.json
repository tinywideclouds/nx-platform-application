{
  "name": "contacts-infrastructure-storage",
  "$schema": "../../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/contacts/infrastructure/storage/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:contacts", "layer:infrastructure"],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/libs/contacts/infrastructure/storage",
        "configFile": "libs/contacts/infrastructure/storage/vite.config.mts"
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": [
        "{options.reportsDirectory}",
        "{options.htmlReportDirectory}"
      ],
      "options": {
        "reportsDirectory": "coverage/libs/contacts/infrastructure/storage",
        "htmlReportDirectory": "dist/test-reports/libs/contacts/infrastructure/storage",
        "configFile": "libs/contacts/infrastructure/storage/vitest.config.mts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
