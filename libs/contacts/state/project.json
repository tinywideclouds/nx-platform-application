{
  "name": "contacts-state",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/contacts/state/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["scope:contacts", "type:state"],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/libs/contacts/state",
        "configFile": "libs/contacts/state/vite.config.mts"
      }
    },
    "test": {
      "executor": "@nx/vitest:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "configFile": "libs/contacts/state/vitest.config.mts",
        "reportsDirectory": "coverage/libs/contacts/sttate"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
