# -----------------------------------------------------------------------------
# EXAMPLE ENVIRONMENT CONFIGURATION FOR THE NODE-MESSAGING-SERVICE
# -----------------------------------------------------------------------------
# Copy this file to .env and fill in your actual, secret values.
# Do NOT commit the real .env file to version control.

# --- Google Cloud Configuration ---
# The ID of your Google Cloud project where the Firestore database lives.
GCP_PROJECT_ID="foobar-test"

IDENTITY_SERVICE_URL="http://localhost:3000"
# --- Security Configuration ---
# A shared secret for server-to-server calls.
# CRITICAL: This MUST match the key in the node-identity-service .env
INTERNAL_API_KEY="a-super-secret-key-for-internal-comms"

# --- Security Configuration ---
# NOW HANDLED BY THE IDENTITY SERVER
# This is the secret key used to VERIFY incoming JWTs.
# CRITICAL: This MUST be the exact same secret that is used in the
# .env file for the `node-identity-service` to SIGN the tokens.
#JWT_SECRET="a-very-long-and-secure-random-string-for-jwt-signing"

# --- Server Configuration (Optional) ---
# The port the service will listen on. Defaults to 3001 if not set.
PORT=3001
